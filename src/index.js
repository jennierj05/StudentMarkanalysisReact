import React from 'react';
import ReactDOM from 'react-dom/client';
import StudentMarkPage from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StudentMarkPage />
  </React.StrictMode>
);


